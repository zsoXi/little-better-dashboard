# Changelog

## [Unreleased]

- Added F8 test skeleton layout only: `tests/` suites, `tools/`
  runners, `docs/` contracts/matrix, CI workflow. All acceptance IDs
  are `NOT_RUN`; no behavior changes to `opencode_dashboard.py`.
